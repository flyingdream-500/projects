<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.networkrequestsproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/network"
        android:label="@string/app_name"
        android:roundIcon="@drawable/network"
        android:supportsRtl="true"
        android:theme="@style/Theme.NetworkRequestsProject">
        <activity
            android:name="com.example.networkrequestsproject.presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>