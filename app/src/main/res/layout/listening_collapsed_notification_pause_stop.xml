<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/root"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:background="@drawable/listening_background">

    <!--Размер текста в dp, что бы вписаться в пропорции с иконками -->
    <TextView
        android:id="@+id/record_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_toStartOf="@id/record_pause"
        android:ellipsize="end"
        android:fontFamily="@font/louis_george_cafe_bold"
        android:maxLines="1"
        android:text="@string/listening_notification_name"
        android:textColor="@color/white"
        android:textSize="16dp" />

    <ImageView
        android:id="@+id/record_pause"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerInParent="true"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="9dp"
        android:src="@drawable/ic_notification_pause"
        android:visibility="visible"
        android:contentDescription="@string/listening_tab" />

    <ImageView
        android:id="@+id/record_stop"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerVertical="true"
        android:layout_marginStart="8dp"
        android:layout_toEndOf="@id/record_pause"
        android:src="@drawable/ic_notification_stop"
        android:visibility="visible"
        android:contentDescription="@string/listening_tab"/>

    <ImageView
        android:id="@+id/exit"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_notification_close"
        android:visibility="visible"
        android:contentDescription="@string/listening_tab"/>


</RelativeLayout>